<?xml version="1.0"?>
<ruleset name="WordPress Plugin Standard">
    <!-- Include WordPress standards -->
    <rule ref="WordPress">
        <!-- Keep the plugin's existing short class-file naming convention; the custom loader strips the shared class prefix. -->
        <exclude name="WordPress.Files.FileName.InvalidClassFileName"/>
        <!-- Treat docblock completeness as a documentation backlog, not a release-blocking runtime lint failure. -->
        <exclude name="Squiz.Commenting.ClassComment.Missing"/>
        <exclude name="Squiz.Commenting.FunctionComment.Missing"/>
        <exclude name="Squiz.Commenting.FunctionComment.MissingParamTag"/>
        <!-- The plugin intentionally handles generated PDFs, CSV streams, and font binaries with native file APIs. -->
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_fclose"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_file_put_contents"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_fopen"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_fread"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_is_writable"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_readfile"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_rmdir"/>
        <!-- Customize exclusions as needed -->
        <exclude name="WordPress.PHP.YodaConditions.NotYoda"/>
    </rule>

    <!-- File exclusions -->
    <exclude-pattern>/vendor/</exclude-pattern>
    <exclude-pattern>/node_modules/</exclude-pattern>
    <exclude-pattern>/tests/</exclude-pattern>
    <exclude-pattern>/assets/dist/</exclude-pattern>

    <!-- Check only PHP files -->
    <arg name="extensions" value="php"/>

    <!-- Show progress -->
    <arg value="p"/>
</ruleset>
